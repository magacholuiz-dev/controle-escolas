{
  "name": "@controle-escolas/contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Request/response types shared by the API and the web app (types only, no runtime code)",
  "types": "src/index.ts",
  "scripts": { "typecheck": "tsc -p tsconfig.json --noEmit" },
  "dependencies": { "@controle-escolas/domain": "*" }
}
