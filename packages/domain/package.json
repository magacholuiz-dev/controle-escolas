{
  "name": "@controle-escolas/domain",
  "version": "1.0.0",
  "private": true,
  "description": "Pure financial domain logic (no Nest, no Mongoose, no React)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "vitest": "^3.2.4"
  }
}
